<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="matrix_view.MatrixView.Buttons">
        <div class="o_matrix_buttons d-flex gap-1 mt-2 mx-3 mb-3 matrix_center">
            <!--Hide the measure button-->
            <!--<div class="btn-group" role="toolbar" aria-label="Main actions">
                <t t-call="web.ReportViewMeasures">
                    <t t-set="measures" t-value="model.metaData.measures"/>
                    <t t-set="activeMeasures" t-value="model.metaData.activeMeasures"/>
                </t>
            </div>-->
            <div class="btn-group" role="toolbar" aria-label="Matrix settings">
                <t t-set="noDataDisplayed" t-value="!model.hasData() || !model.metaData.activeMeasures.length"/>
                <!--Hide the Flip axis button-->
                <!--<button class="btn btn-secondary fa fa-exchange o_matrix_flip_button" t-on-click="onFlipButtonClicked" data-tooltip="Flip axis" aria-label="Flip axis" t-attf-disabled="{{noDataDisplayed ? 'disabled' : false}}"/>-->
                <!--Hide the expand button-->
                <!--<button class="btn btn-secondary fa fa-arrows o_matrix_expand_button" t-on-click="onExpandButtonClicked" data-tooltip="Expand all" aria-label="Expand all" t-attf-disabled="{{noDataDisplayed ? 'disabled' : false}}"/>-->
                <button class="btn btn-primary o_matrix_edit matrix_top_button" t-on-click="onEditButtonClicked" data-tooltip="Edit" aria-label="Edit" style="display:none;margin-right:5px;">Edit</button>
                <button class="btn btn-primary o_matrix_save matrix_top_button matrix-btn-primary" t-on-click="onSaveButtonClicked" data-tooltip="Save" aria-label="Save" style="margin-right:5px;">Save</button>
                <button class="btn btn-secondary o_matrix_cancel matrix_top_button matrix-btn-secondary" t-on-click="onCancelButtonClicked" data-tooltip="Cancel" aria-label="Cancel" style="margin-right:5px;">Cancel</button>
                <button class="btn btn-secondary fa fa-download o_matrix_download" t-on-click="onDownloadButtonClicked" data-tooltip="Download xlsx" 
                    aria-label="Download xlsx" t-attf-disabled="{{noDataDisplayed ? 'disabled' : false}}" style="display:none;"/>
            </div>
        </div>
    </t>

    <t t-name="matrix_view.MatrixView">
        <div t-att-class="props.className" t-ref="root">
            <Layout className="model.useSampleModel ? 'o_view_sample_data' : ''" display="props.display">
                <t t-set-slot="control-panel-additional-actions">
                    <CogMenu/>
                </t>
                <t t-set-slot="layout-actions">
                    <SearchBar t-if="searchBarToggler.state.showSearchBar"/>
                </t>
                <t t-set-slot="control-panel-navigation-additional">
                    <t t-component="searchBarToggler.component" t-props="searchBarToggler.props"/>
                </t>
                <t t-set="displayNoContent" t-value="
                    props.info.noContentHelp !== false and (
                        !(model.hasData() and model.metaData.activeMeasures.length) or
                        model.useSampleModel
                    )"
                />
                <t t-if="displayNoContent">
                    <t t-if="props.info.noContentHelp" t-call="web.ActionHelper">
                        <t t-set="noContentHelp" t-value="props.info.noContentHelp"/>
                    </t>
                    <t t-else="" t-call="web.NoContentHelper"/>
                </t>
                <t t-component="props.Renderer" model="model" buttonTemplate="props.buttonTemplate"/>
            </Layout>
        </div>
    </t>

</templates>
